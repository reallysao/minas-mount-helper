# 小米智能存储 Finder 挂载助手

一个 macOS 本地应用：把「小米智能存储」NAS 直接挂载到 Mac 的 Finder，
**异地（NAS 不在本机局域网）也能正常浏览、上传、下载、打开文件**。

## 原理（无需修改 NAS / 路由器）

```
小米NAS ──P2P隧道──▶ 官方App(sso_login 原生进程, 本地动态端口)
                          │
                          ▼
              本地反向代理 http://127.0.0.1:18445
              （自动注入凭证，全方法透传 DAV 1,2）
                          │
                          ▼
              Finder NetFS 原生挂载 → /Volumes/127.0.0.1
```

- 复用官方 App 已建立的 P2P 隧道（`sso_login` 进程），不做任何网络穿透配置；
- 隧道端口是动态的，应用每 20 秒自动探测，端口变了自动跟随；
- WebDAV 凭证从官方 App 的本地缓存（LevelDB）提取，无需手工输入；
- 本地代理是独立进程，App 退出/重启后已挂载的卷不受影响；
- 代理会过滤 PROPFIND 响应中的"目录自引用"条目，避免 Finder 出现幽灵文件夹
  （如挂载根里多出一个 `data` 文件夹、子文件夹里出现母文件夹）。

## 安装

1. 确保已登录并运行官方「小米智能存储」App（必须，隧道依赖它）；
2. 打开 `/Applications/小米智能存储挂载助手.app`；
3. 状态变为「在线」后点 **挂载**（勾选「连接后自动挂载」则自动完成）；
4. Finder 侧边栏出现 `127.0.0.1`，即可像本地磁盘一样使用。

> 首次打开如被 Gatekeeper 拦截：右键应用 → 打开 → 再点「打开」。

## 界面功能

- **存储空间**：已用 / 总量 + 使用率进度条；
- **连接**：P2P 隧道状态与当前 WebDAV 端口（端口变化自动跟踪）；
- **Finder 挂载**：挂载 / 卸载 / 在 Finder 中打开；
- **连接后自动挂载**：隧道可用时自动挂载（含断线自动重连、死挂载自动清理重挂）；
- **登录时自动启动**：写入 LaunchAgent，开机自动运行并挂载。

## 修复记录（2026-09-08）

| 问题 | 根因 | 修复 |
|---|---|---|
| 频繁弹"需要重新连接"、挂载经常失败 | 官方 App 缓存中密码字段被 LevelDB 正则误吞入相邻 JSON（`"port_2":58313`），导致代理注入错误凭证，NAS 全部返回 401 | 凭证提取在首个引号处截断并过滤 JSON 特征；代理加载状态文件时校验密码合法性，脏数据直接忽略 |
| 挂载根目录出现名为 `data` 的幽灵文件夹、子文件夹里出现母文件夹 | macOS WebDAV 客户端把 PROPFIND 响应里"目录自身"条目渲染成可见子目录 | 代理层剥离列表响应中的自引用条目（仅多条目列表时，Depth:0 不受影响） |
| 应用"登录时自动启动"失效 | LaunchAgent 指向 `Contents/Resources/launcher.sh`，而启动器实际在 `Contents/MacOS/` | 启动器补齐到 Resources（两处均可运行） |
| 断线后挂载长期"假死"、反复手动重连 | 隧道抖动后挂载仍挂在表里但上游已失效 | 新增挂载健康自愈：连续 2 次探活失败自动卸载清理（消除 macOS 弹窗），恢复后自动重挂 |

## 性能优化（2026-09-09）

针对"大文件夹长时间'正在载入……'"和"Finder 卡死"问题，代理升级到 v2 优化版：

| 优化项 | 优化前 | 优化后 | 效果 |
|---|---|---|---|
| 上游连接复用 | 每个请求新建 TLS 连接，重复握手 | 每线程持久连接，自动复用 | 大目录减少大量 TLS 握手延迟 |
| Finder 元数据请求 | `._文件名`/`.DS_Store` 等全部转发上游，等待 404 | 代理层直接返回 404，不转发 | 单个请求从 ~0.2s 降到 0.001s（200 倍），大目录几百个文件累积节省巨大 |
| PROPFIND 响应缓存 | 每次重复请求同一目录都重新转发上游 | 大目录列表缓存 8 秒，重复请求直接返回 | 切换视图/刷新时秒开 |
| 上游并发限制 | 无限制，Finder 大目录同时发几百请求打爆上游 | 信号量限制最大 12 并发 | 避免上游拥塞，提高稳定性 |
| 上游超时 | 30 秒（Finder 长时间卡死） | 12 秒 | 慢请求更快失败，减少 Finder 卡死时间 |
| 请求体读取容错 | 异常 Content-Length 导致线程崩溃 OSError | 校验长度范围 + try-except 容错 | 避免代理线程异常退出 |

**Finder 卡死的额外说明**：macOS 的 webdavfs 内核扩展在大目录/网络抖动时本身有稳定性问题，代理优化可大幅缓解但无法 100% 消除。若仍偶发卡死，可在 Finder 中按 ⌘+句号 显示隐藏文件后删除挂载卷上的 `.DS_Store`，或重启 Finder（⌥+右键 Dock 上的 Finder → 重新开启）。

## Bug 修复（2026-09-10）

| 问题 | 根因 | 修复 |
|---|---|---|
| 新建文件夹后无法重命名，Finder 提示"请尝试使用字符较少，或不含标点符号的名称"（中文文件名也报错） | MOVE/COPY 请求的 `Destination` 头未被代理转换：Finder 发的是 `http://127.0.0.1:18445/路径/`，代理直接转发给 NAS，NAS 不认识本地代理地址，返回 400 Bad Request | 代理层在转发前转换 `Destination` 头：解析 URL 提取路径，重新构造为 `https://上游:端口/pool0/data/路径/`，保留 query 和 fragment |
| 批量删除文件慢 | Finder 串行发送 DELETE 请求，每个请求需等待上游 NAS 响应（~0.17s/个），删除 N 个文件需 N×0.17s | 代理已优化连接复用（避免每次 TLS 握手）、并发限制、超时缩短；当前 ~0.17s/个，20个文件约 3.7s。进一步优化需 Finder 支持并发删除（代理无法改变 Finder 串行行为） |

## 文件位置

| 内容 | 路径 |
|---|---|
| 应用 | `/Applications/小米智能存储挂载助手.app` |
| 源码工程 | `~/Doubao/chats/2026-09-07/new-chat-1/mountapp/` |
| 运行日志 | `~/Library/Application Support/小米智能存储挂载助手/proxy.log` |
| 代理状态 | `~/Library/Application Support/小米智能存储挂载助手/proxy_state.json` |

## 安全说明（在外部设备上使用前必读）

**结论：局域网/其他电脑无法扫描到你的 NAS；本机其他用户账户也无法访问。主要风险是「同一登录会话内的人」和「设备管理员权限」。**

| 威胁场景 | 状态 | 说明 |
|---|---|---|
| 外部局域网其他电脑扫描/访问 | ✅ 安全 | 代理只监听 `127.0.0.1`（本机回环），不对外开任何端口；NAS 本身不在外部网络 |
| 同机其他用户账户（如切换用户） | ✅ 已加固 | 代理强制凭证校验（无凭证/错误凭证一律 401）；所有含密码文件已 `chmod 600/700`，仅当前用户可读 |
| 同事使用你已解锁的电脑 | ⚠️ 可访问 | Finder 侧边栏直接可见挂载卷，任何人可浏览/编辑。**离开前务必锁屏（⌃⌘Q）** |
| 设备管理员（MDM/管理员权限） | ⚠️ 理论上可读 | 管理员本就可访问该电脑一切内容（含官方 App 自身缓存的明文凭证），这是任何软件都无法规避的；**敏感个人文件尽量不长期存放在外部设备上** |
| 网络传输内容 | ✅ 加密 | 隧道为 TLS 加密连接，外部网络只能看到加密流量，看不到文件内容 |

**应用自身已做的加固：**
- 本地代理强制入站 Basic 认证（只有携带 NAS 凭证的请求放行，Finder 挂载时自动携带）
- 代理状态文件（含密码）权限 `600`、目录权限 `700`
- 官方 App 的客户端私钥由 `644` 收紧为 `600`（原为所有用户可读）
- 挂载仅存在于你的登录会话，不导出任何共享服务

**建议习惯：**
1. 离开工位锁屏；不要把自己的 macOS 账户借给别人
2. 不用时在应用里点「卸载」；敏感内容用完即取
3. 外部设备上建议不勾「登录时自动启动」，需要时手动打开
4. 最敏感的个人文件（证件、财务、私密照片）不建议存放在外部托管设备可访问的位置

## 已知限制

- 需要官方「小米智能存储」App 正在运行（P2P 隧道由它维护）；
- 隧道偶发瞬时抖动（几秒），稍后自动恢复；期间 Finder 可能短暂无响应；
- 移动端/远端设备需保持在线，NAS 关机则无法访问；
- 本应用为第三方工具，非小米官方出品。
